# Bakery Route — Flask Skeleton
This repository is a minimal, well-structured Flask application to be used as
the foundation for the Bakery Route app.
## Quick start
1. Create and activate a Python virtual environment (Python 3.10+ recommended):
```bash
python -m venv .venv
source .venv/bin/activate # macOS/Linux
.\.venv\Scripts\activate # Windows (PowerShell)
